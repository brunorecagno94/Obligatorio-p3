@model IEnumerable<WebClient.Models.DTOs.Envio.EnvioCompletoListadoDTO>?

@{
    ViewData["Title"] = "Buscar Envíos por fecha";
}

<h2>Buscar Envíos por fecha</h2>

<form asp-action="BuscarPorFechaYEstado" method="post" class="flex">
    <div class="form-group">
        <label for="fecha1">Desde:</label>
        <input type="date" class="form-control" id="fecha1" name="fecha1" required />
    </div>
    <div class="form-group">
        <label for="fecha2">Hasta:</label>
        <input type="date" class="form-control" id="fecha2" name="fecha2" required />
    </div>
    <div class="form-group">
        <label for="fecha1">Estado:</label>
        <select class="form-control" id="estado" name="estado">
            <option value="">Seleccione un estado</option>
            <option value="EN_PROCESO">En proceso</option>
            <option value="FINALIZADO">Finalizado</option>
        </select>
    </div>
    <button type="submit" class="btn btn-primary mt-2">Buscar</button>
</form>

@if (ViewBag.Mensaje != null)
{
    <div class="alert alert-warning mt-3">@ViewBag.Mensaje</div>
}
else if (Model != null && Model.Any())
{
    <hr />
    <h4>Resultados</h4>
    <table class="table table-striped mt-3">
        <thead>
            <tr>
                <th>ID</th>
                <th>Tracking</th>
                <th>Urgente</th>
                <th>Fecha Salida</th>
                <th>Estado</th>
                <th>Comentarios</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var envio in Model)
            {
                <tr>
                    <td>@envio.Id</td>
                    <td>@envio.NumeroTracking</td>
                    <td>@(envio.EsUrgente ? "Sí" : "No")</td>
                    <td>@envio.Fecha.ToShortDateString()</td>
                    <td>@envio.Estado</td>
                    <td>
                        @if (envio.Comentarios != null && envio.Comentarios.Any())
                        {
                            <ul class="mb-0">
                                @foreach (var comentario in envio.Comentarios)
                                {
                                    <li><small><strong>@comentario.FechaComentario.ToShortDateString():</strong> @comentario.TextoComentario</small></li>
                                }
                            </ul>
                        }
                        else
                        {
                            <em>Sin comentarios</em>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
}